package com.dev.fitface.utils

class Constants {
    class ActionMain{
        companion object {
            const val callNextAction = "callNextAction"
            const val callAction = "callAction"
        }
    }
}